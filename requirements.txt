# AI与语言处理
langchain==0.3.27
langgraph==0.6.10
transformers>=4.30.0
torch>=2.0.0

# Web框架
flask>=2.0.0
flask-cors>=3.0.10
werkzeug>=2.3.0

# OpenAI API 客户端
openai>=1.0.0
httpx>=0.24.0  # 用于HTTP客户端请求

# 视频处理相关库
moviepy>=1.0.3
opencv-python>=4.8.0  # 用于计算机视觉功能，场景识别和内容分析
ffmpeg-python>=0.2.0  # Python的FFmpeg接口
pydub>=0.25.0  # 音频处理库

# 语音识别
SpeechRecognition>=3.14.0  # 语音识别库
librosa>=0.10.0  # 音频分析库
pipwin>=0.5.1  # Windows用户安装音频依赖

# 数据分析与科学计算
numpy>=1.23.0,<2.3.0  # 确保与opencv-python-headless兼容
pandas>=2.0.0
scikit-learn>=1.3.0

# 日志处理
python-json-logger>=2.0.0
colorama>=0.4.6  # 控制台彩色输出

# 环境变量管理
python-dotenv>=1.0.0

# YAML配置文件处理
PyYAML>=6.0

# 其他依赖
requests>=2.25.0,<3.0.0  # 保持稳定版本
urllib3<2.1,>=1.25.4  # 与botocore兼容

tqdm>=4.60.0  # 进度条显示

# 注意：此项目需要在系统上安装FFmpeg
# Windows用户：可以从 https://ffmpeg.org/download.html 下载并添加到PATH
# Linux用户：可以使用包管理器安装 (apt install ffmpeg 或 yum install ffmpeg)
# Mac用户：可以使用Homebrew安装 (brew install ffmpeg)